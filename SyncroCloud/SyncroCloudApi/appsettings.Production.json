{
  // The production connection string is supplied by the Azure App Service setting
  // ConnectionStrings__DefaultConnection (Configuration > Connection strings / Application settings).
  // Do not put the real host/username/password here.
  "ConnectionStrings": {
    "DefaultConnection": ""
  }
}
